int reverse(int x){
    long dest = 0;
    while (x != 0) {
        dest = dest * 10 + x % 10;
        x = x / 10;
    }
    if ((int)dest != dest)  
        return 0;
    else
        return (int)dest;
}
